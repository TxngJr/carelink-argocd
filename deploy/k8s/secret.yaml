apiVersion: v1
kind: Secret
metadata:
  name: carelink-config
type: Opaque
stringData:
  # Prototype/class project values are intentionally checked in for one-command deploy.
  MONGO_URI: mongodb://mongo-0.mongo:27017/?replicaSet=rs0
  DB_NAME: carelink
  JWT_SECRET: carelink-demo-jwt-secret-denmannsolutions-2026
  APP_ENV: development
  NEXT_PUBLIC_APP_NAME: CareLink
