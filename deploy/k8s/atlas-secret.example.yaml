# Example only. Do NOT commit a live MongoDB Atlas password to this public repository.
# Create this Secret directly in the cluster instead; see deploy/k8s/README.md.
apiVersion: v1
kind: Secret
metadata:
  name: carelink-atlas
  namespace: carelink
type: Opaque
stringData:
  MONGO_URI: "mongodb+srv://txngjr:<ATLAS_PASSWORD>@cluster0.tha54x2.mongodb.net/carelink?retryWrites=true&w=majority"
